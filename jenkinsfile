pipeline {
    agent any 

    stages {
        stage('Check') { 
            steps { 
                sh './gradlew check' 
            }
        }
        stage('Test'){
            steps {
                junit 'reports/**/*.xml' 
            }
        }
    }
}
